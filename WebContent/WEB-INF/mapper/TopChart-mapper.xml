<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="topchart">
	<select id="top" resultType="java.util.HashMap">
	<![CDATA[
		select rownum, temp.* from
   		(select * from fileupload order by count desc) temp 
		where rownum <=10
	]]>
	</select>
	<select id="toppop" resultType="java.util.HashMap">
	<![CDATA[
		select rownum, temp.* from
   		(select * from fileupload order by count desc) temp 
		where rownum <=10 AND genre='pop'
	]]>
	</select>
	<select id="toplock" resultType="java.util.HashMap">
	<![CDATA[
		select rownum, temp.* from
   		(select * from fileupload order by count desc) temp 
		where rownum <=10 AND genre='lock'
	]]>
	</select>
	<select id="topjazz" resultType="java.util.HashMap">
	<![CDATA[
		select rownum, temp.* from
   		(select * from fileupload order by count desc) temp 
		where rownum <=10 AND genre='jazz'
	]]>
	</select>
	<select id="tophiphop" resultType="java.util.HashMap">
	<![CDATA[
		select rownum, temp.* from
   		(select * from fileupload order by count desc) temp 
		where rownum <=10 AND genre='hiphop'
	]]>
	</select>
	<select id="topcountry" resultType="java.util.HashMap">
	<![CDATA[
		select rownum, temp.* from
   		(select * from fileupload order by count desc) temp 
		where rownum <=10 AND genre='country'
	]]>
	</select>
	<select id="topelectronic" resultType="java.util.HashMap">
	<![CDATA[
		select rownum, temp.* from
   		(select * from fileupload order by count desc) temp 
		where rownum <=10 AND genre='electronic'
	]]>
	</select>
	
</mapper>
 