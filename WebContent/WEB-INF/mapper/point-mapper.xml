<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="point">

	<select id="viewPoint" parameterType="java.lang.String"
		resultType="java.lang.String">
		select point from member where id = #{id}
	</select>
	
	<update id="updatePoint" parameterType="java.util.HashMap">
		update member set point = point+#{point} where id=#{id}
	</update>
	
	<select id="viewPointB" parameterType="java.lang.String"
		resultType="java.lang.String">
		select point from band where id = #{id}
	</select>
	
	<update id="updatePointB" parameterType="java.util.HashMap">
		update band set point = point+#{point} where id=#{id}
	</update>
</mapper>